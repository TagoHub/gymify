.title
  %h1 My Programs
  = link_to new_program_path(), class: "btn btn-outline-primary btn-sm" do
    %i.bi.bi-plus.h4
.index
  %table
    %thead
      %tr
        %th Name
        %th Frequency
        %th Level
        %th Goal
        %th Duration
        %th Time Unit
        %th Actions
    %tbody
      - @programs.each do |program|
        %tr
          %td= program.name
          %td= program.frequency
          %td= program.level
          %td= program.goal
          %td= program.duration
          %td= program.unit&.abbreviation
          %td.actions
            = link_to program_path(program), class: "btn" do
              %i.bi.bi-eye
            - if current_user.programs.include?(program)
              = link_to edit_program_path(program), class: "btn" do
                %i.bi.bi-pencil
              = button_to program_path(program), method: :delete, data: { confirm: "Are you sure?" }, class: "btn delete link" do
                %i.bi.bi-trash